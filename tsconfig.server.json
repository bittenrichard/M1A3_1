{
  "compilerOptions": {
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "target": "ES2022",
    "lib": ["esnext"],
    "outDir": "./dist",
    "rootDir": ".",
    "esModuleInterop": true,
    "strict": true,
    "noEmit": false,
    "allowSyntheticDefaultImports": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "baseUrl": ".",
    "paths": {
      "~/*": ["./src/*"]
    }
  },
  // A MUDANÇA CRÍTICA:
  // Forçamos o compilador a usar APENAS o server.ts como ponto de partida.
  // Ele irá seguir as importações a partir daqui, ignorando qualquer outro
  // arquivo .ts no projeto que não faça parte da dependência do servidor.
  "files": [
    "server.ts"
  ],
  // 'include' e 'exclude' são removidos para evitar conflitos com 'files'.
}